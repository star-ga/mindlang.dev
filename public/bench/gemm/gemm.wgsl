// GEMM Benchmark - Compiled WGSL Shader
// Generated by MIND Compiler v0.2.0
// Copyright (c) 2026 STARGA Inc. - MIT License
//
// High-performance register-tiled matrix multiplication.
// 16x16 workgroups, 8x4 output per thread = 128x64 output per workgroup.
// Bank-conflict-free shared memory via stride-17 padding on A tile.
// Vectorized vec4 global memory access for A, B, and C.
// Bounds checks elided — requires M % 128 == 0, N % 64 == 0, K % 16 == 0.

struct GemmParams {
    M: u32,
    N: u32,
    K: u32,
    alpha: f32,
}

@group(0) @binding(0) var<uniform> params: GemmParams;
@group(0) @binding(1) var<storage, read> A: array<vec4<f32>>;
@group(0) @binding(2) var<storage, read> B: array<vec4<f32>>;
@group(0) @binding(3) var<storage, read_write> C: array<vec4<f32>>;

// Shared memory tiles
// sa: 128 rows x 16 K-cols, padded stride 17 (eliminates 32-bank conflicts)
// sb: 16 K-rows x 64 cols
// Total shared: (2176 + 1024) * 4 = 12800 bytes
var<workgroup> sa: array<f32, 2176>;
var<workgroup> sb: array<f32, 1024>;

@compute @workgroup_size(16, 16, 1)
fn main(@builtin(local_invocation_id) lid: vec3<u32>,
        @builtin(workgroup_id) wid: vec3<u32>) {
    let K = params.K;
    let N4 = params.N / 4u;
    let K4 = K / 4u;

    let tx = lid.x;
    let ty = lid.y;
    let tid = ty * 16u + tx;

    // Base row/col for this workgroup's 128x64 output tile
    let wg_row = wid.y * 128u;
    let wg_col = wid.x * 64u;

    // 32 register accumulators (8 rows x 4 cols per thread)
    var r0c0: f32 = 0.0; var r0c1: f32 = 0.0; var r0c2: f32 = 0.0; var r0c3: f32 = 0.0;
    var r1c0: f32 = 0.0; var r1c1: f32 = 0.0; var r1c2: f32 = 0.0; var r1c3: f32 = 0.0;
    var r2c0: f32 = 0.0; var r2c1: f32 = 0.0; var r2c2: f32 = 0.0; var r2c3: f32 = 0.0;
    var r3c0: f32 = 0.0; var r3c1: f32 = 0.0; var r3c2: f32 = 0.0; var r3c3: f32 = 0.0;
    var r4c0: f32 = 0.0; var r4c1: f32 = 0.0; var r4c2: f32 = 0.0; var r4c3: f32 = 0.0;
    var r5c0: f32 = 0.0; var r5c1: f32 = 0.0; var r5c2: f32 = 0.0; var r5c3: f32 = 0.0;
    var r6c0: f32 = 0.0; var r6c1: f32 = 0.0; var r6c2: f32 = 0.0; var r6c3: f32 = 0.0;
    var r7c0: f32 = 0.0; var r7c1: f32 = 0.0; var r7c2: f32 = 0.0; var r7c3: f32 = 0.0;

    let num_tiles = K / 16u;

    for (var t: u32 = 0u; t < num_tiles; t++) {
        let k_off = t * 16u;

        // ---- Load sa[128][16] via 2 vec4 loads from A per thread ----
        // 2048 elements / 256 threads = 8 per thread = 2 vec4 loads
        for (var i: u32 = 0u; i < 2u; i++) {
            let base = tid * 8u + i * 4u;
            let row = base / 16u;
            let col = base & 15u;
            let v = A[(wg_row + row) * K4 + (k_off + col) / 4u];
            let si = row * 17u + col;
            sa[si]      = v.x;
            sa[si + 1u] = v.y;
            sa[si + 2u] = v.z;
            sa[si + 3u] = v.w;
        }

        // ---- Load sb[16][64] via 1 vec4 load from B per thread ----
        // 1024 elements / 256 threads = 4 per thread = 1 vec4 load
        {
            let base = tid * 4u;
            let row = base / 64u;
            let col = base & 63u;
            let v = B[(k_off + row) * N4 + (wg_col + col) / 4u];
            let si = row * 64u + col;
            sb[si]      = v.x;
            sb[si + 1u] = v.y;
            sb[si + 2u] = v.z;
            sb[si + 3u] = v.w;
        }

        workgroupBarrier();

        // ---- 8x4 outer-product accumulation (32 FMAs per k) ----
        let sa_row0 = ty * 8u;
        for (var k: u32 = 0u; k < 16u; k++) {
            let a0 = sa[(sa_row0 + 0u) * 17u + k];
            let a1 = sa[(sa_row0 + 1u) * 17u + k];
            let a2 = sa[(sa_row0 + 2u) * 17u + k];
            let a3 = sa[(sa_row0 + 3u) * 17u + k];
            let a4 = sa[(sa_row0 + 4u) * 17u + k];
            let a5 = sa[(sa_row0 + 5u) * 17u + k];
            let a6 = sa[(sa_row0 + 6u) * 17u + k];
            let a7 = sa[(sa_row0 + 7u) * 17u + k];

            let sb_off = k * 64u + tx * 4u;
            let b0 = sb[sb_off];
            let b1 = sb[sb_off + 1u];
            let b2 = sb[sb_off + 2u];
            let b3 = sb[sb_off + 3u];

            r0c0 += a0 * b0; r0c1 += a0 * b1; r0c2 += a0 * b2; r0c3 += a0 * b3;
            r1c0 += a1 * b0; r1c1 += a1 * b1; r1c2 += a1 * b2; r1c3 += a1 * b3;
            r2c0 += a2 * b0; r2c1 += a2 * b1; r2c2 += a2 * b2; r2c3 += a2 * b3;
            r3c0 += a3 * b0; r3c1 += a3 * b1; r3c2 += a3 * b2; r3c3 += a3 * b3;
            r4c0 += a4 * b0; r4c1 += a4 * b1; r4c2 += a4 * b2; r4c3 += a4 * b3;
            r5c0 += a5 * b0; r5c1 += a5 * b1; r5c2 += a5 * b2; r5c3 += a5 * b3;
            r6c0 += a6 * b0; r6c1 += a6 * b1; r6c2 += a6 * b2; r6c3 += a6 * b3;
            r7c0 += a7 * b0; r7c1 += a7 * b1; r7c2 += a7 * b2; r7c3 += a7 * b3;
        }

        workgroupBarrier();
    }

    // ---- Write 8x4 output block via vec4 stores ----
    let alpha = params.alpha;
    let or0 = wg_row + ty * 8u;
    let oc4 = wg_col / 4u + tx;

    C[(or0 + 0u) * N4 + oc4] = alpha * vec4<f32>(r0c0, r0c1, r0c2, r0c3);
    C[(or0 + 1u) * N4 + oc4] = alpha * vec4<f32>(r1c0, r1c1, r1c2, r1c3);
    C[(or0 + 2u) * N4 + oc4] = alpha * vec4<f32>(r2c0, r2c1, r2c2, r2c3);
    C[(or0 + 3u) * N4 + oc4] = alpha * vec4<f32>(r3c0, r3c1, r3c2, r3c3);
    C[(or0 + 4u) * N4 + oc4] = alpha * vec4<f32>(r4c0, r4c1, r4c2, r4c3);
    C[(or0 + 5u) * N4 + oc4] = alpha * vec4<f32>(r5c0, r5c1, r5c2, r5c3);
    C[(or0 + 6u) * N4 + oc4] = alpha * vec4<f32>(r6c0, r6c1, r6c2, r6c3);
    C[(or0 + 7u) * N4 + oc4] = alpha * vec4<f32>(r7c0, r7c1, r7c2, r7c3);
}
